{
  "rules": {
    "app": {
      // The API key can be read by any authenticated user.
      "apiKey": {
        ".read": "auth != null",
        ".write": false
      },
      // Authenticated users can control the switches.
      "switchStates": {
        ".read": "auth != null",
        ".write": "auth != null"
      },
      // Energy data can be read by authenticated users, but only written by the server.
      "energyData": {
        ".read": "auth != null",
        ".write": false
      }
    }
  }
}
